---
phase: 02-ai-coordinator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/config.py
  - backend/schemas/coordinator.py
  - backend/tests/__init__.py
  - backend/tests/test_ai_coordinator.py
autonomous: true
requirements: [REQ-06, REQ-07, REQ-08, REQ-09]

must_haves:
  truths:
    - "anthropic SDK is installable and listed in requirements.txt"
    - "config.py has ANTHROPIC_API_KEY and AI_QUALITY_THRESHOLD fields"
    - "Pydantic v2 schemas exist for AgentInfo, AgentCall, QualityEvaluation"
    - "Test file exists with failing tests for all 4 requirements"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "anthropic, pytest, pytest-asyncio dependencies"
      contains: "anthropic"
    - path: "backend/config.py"
      provides: "AI coordinator settings fields"
      contains: "ANTHROPIC_API_KEY"
    - path: "backend/schemas/coordinator.py"
      provides: "Pydantic models for AI coordinator"
      exports: ["AgentInfo", "AgentCall", "QualityEvaluation", "CoordinatorError"]
    - path: "backend/tests/test_ai_coordinator.py"
      provides: "Test scaffold for all REQ-06 through REQ-09"
      min_lines: 50
  key_links:
    - from: "backend/schemas/coordinator.py"
      to: "pydantic"
      via: "BaseModel import"
      pattern: "from pydantic import BaseModel"
    - from: "backend/config.py"
      to: ".env"
      via: "pydantic_settings"
      pattern: "ANTHROPIC_API_KEY"
---

<objective>
Set up the foundation for AI Coordinator: install dependencies, add config fields, create Pydantic schemas, and write test scaffold with failing tests.

Purpose: Establish contracts (types, config) and test expectations before implementing the coordinator service. All subsequent work builds on these schemas and passes these tests.
Output: requirements.txt updated, config.py extended, schemas/coordinator.py created, tests/test_ai_coordinator.py with failing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-coordinator/02-RESEARCH.md
@backend/config.py
@backend/requirements.txt
@backend/schemas/execution.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and config fields</name>
  <files>backend/requirements.txt, backend/config.py</files>
  <read_first>
    - backend/requirements.txt (current dependencies list)
    - backend/config.py (current Settings class with pydantic_settings pattern)
  </read_first>
  <action>
1. Append these lines to `backend/requirements.txt` (after existing entries):
```
anthropic>=0.40.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
```

2. In `backend/config.py`, add two new fields to the `Settings` class, inside a new comment section AFTER the `# ─── Platform` section (before the closing of the class):
```python
    # ─── AI Координатор ─────────────────────────────────────────────
    ANTHROPIC_API_KEY: str = ""
    AI_QUALITY_THRESHOLD: int = 70
```

These follow the exact same pattern as `PLATFORM_WALLET_ADDRESS: str = ""` — optional string with empty default so the app doesn't crash when env var is missing (it only crashes when ai_coordinator.py is actually called).
  </action>
  <verify>
    <automated>cd /Users/33karaulov/Documents/decentrathon5.0 && grep -c "anthropic" backend/requirements.txt && grep -c "ANTHROPIC_API_KEY" backend/config.py && grep -c "AI_QUALITY_THRESHOLD" backend/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "anthropic>=0.40.0" backend/requirements.txt` returns a match
    - `grep "pytest>=8.0.0" backend/requirements.txt` returns a match
    - `grep "pytest-asyncio>=0.24.0" backend/requirements.txt` returns a match
    - `grep "ANTHROPIC_API_KEY: str" backend/config.py` returns a match
    - `grep "AI_QUALITY_THRESHOLD: int = 70" backend/config.py` returns a match
    - The two new fields are inside the `Settings` class (indented with 4 spaces)
  </acceptance_criteria>
  <done>requirements.txt has anthropic + test deps; config.py has ANTHROPIC_API_KEY and AI_QUALITY_THRESHOLD fields</done>
</task>

<task type="auto">
  <name>Task 2: Create Pydantic v2 schemas for coordinator</name>
  <files>backend/schemas/coordinator.py</files>
  <read_first>
    - backend/schemas/execution.py (existing schema pattern — imports, BaseModel usage, ConfigDict)
    - backend/schemas/agent.py (if exists — agent schema patterns)
  </read_first>
  <action>
Create `backend/schemas/coordinator.py` with these exact classes:

```python
"""Схемы AI координатора — модели для route_task() и evaluate_output()."""

from typing import Any

from pydantic import BaseModel, Field


class AgentInfo(BaseModel):
    """Информация об агенте для передачи в координатор."""
    slug: str
    name: str
    description: str | None = None
    capabilities: list[str] = []
    price_per_call: str  # Decimal как строка для JSON-совместимости


class AgentCall(BaseModel):
    """Один шаг в пайплайне агентов — результат route_task()."""
    slug: str
    input: dict[str, Any]
    reason: str  # почему Claude выбрал этого агента


class QualityEvaluation(BaseModel):
    """Результат evaluate_output() — оценка качества выполнения."""
    score: int = Field(ge=0, le=100)
    reasoning: str
    decision: str  # "complete" | "refund"
    execution_id: str


class CoordinatorError(Exception):
    """Ошибка AI координатора — вызывающий код должен выполнить refund."""
    pass
```

All comments in Russian. Uses Pydantic v2 syntax (no `class Config`, uses `Field` from pydantic). `QualityEvaluation.score` has `ge=0, le=100` constraint for u8-safe range. `CoordinatorError` is a custom exception for Claude API failures.
  </action>
  <verify>
    <automated>cd /Users/33karaulov/Documents/decentrathon5.0/backend && python3 -c "from schemas.coordinator import AgentInfo, AgentCall, QualityEvaluation, CoordinatorError; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/schemas/coordinator.py` exists
    - `grep "class AgentInfo(BaseModel)" backend/schemas/coordinator.py` returns a match
    - `grep "class AgentCall(BaseModel)" backend/schemas/coordinator.py` returns a match
    - `grep "class QualityEvaluation(BaseModel)" backend/schemas/coordinator.py` returns a match
    - `grep "class CoordinatorError(Exception)" backend/schemas/coordinator.py` returns a match
    - `grep "score: int = Field(ge=0, le=100)" backend/schemas/coordinator.py` returns a match
    - `cd backend && python3 -c "from schemas.coordinator import AgentInfo, AgentCall, QualityEvaluation, CoordinatorError; print('OK')"` exits 0
    - `QualityEvaluation(score=70, reasoning='ok', decision='complete', execution_id='x')` validates without error (boundary check)
  </acceptance_criteria>
  <done>schemas/coordinator.py exists with AgentInfo, AgentCall, QualityEvaluation, CoordinatorError — all importable</done>
</task>

<task type="auto">
  <name>Task 3: Create test scaffold with failing tests</name>
  <files>backend/tests/__init__.py, backend/tests/test_ai_coordinator.py</files>
  <read_first>
    - backend/schemas/coordinator.py (the schemas just created — import paths)
    - backend/config.py (settings import path)
  </read_first>
  <action>
1. Create `backend/tests/__init__.py` as an empty file (just a comment):
```python
# Тесты для backend сервисов
```

2. Create `backend/tests/test_ai_coordinator.py` with tests that cover all 4 requirements. Tests mock the Claude API so no real API key needed. The tests should FAIL because `backend/services/ai_coordinator.py` does not exist yet:

```python
"""Тесты AI координатора — покрывают REQ-06, REQ-07, REQ-08, REQ-09."""

import json
from unittest.mock import AsyncMock, MagicMock, patch

import pytest

from schemas.coordinator import (
    AgentCall,
    AgentInfo,
    CoordinatorError,
    QualityEvaluation,
)


# ─── Фикстуры ───────────────────────────────────────────────────


@pytest.fixture
def sample_agents() -> list[AgentInfo]:
    """Тестовые агенты для route_task."""
    return [
        AgentInfo(
            slug="pdf-summarizer",
            name="PDF Summarizer",
            description="Summarizes PDF documents",
            capabilities=["pdf", "summarization"],
            price_per_call="1000000",
        ),
        AgentInfo(
            slug="ru-translator",
            name="Russian Translator",
            description="Translates text to Russian",
            capabilities=["translation", "russian"],
            price_per_call="500000",
        ),
    ]


def _mock_claude_response(text: str) -> MagicMock:
    """Создаёт мок ответа Claude API."""
    content_block = MagicMock()
    content_block.text = text
    response = MagicMock()
    response.content = [content_block]
    return response


# ─── REQ-06: route_task() возвращает pipeline агентов ────────────


@pytest.mark.asyncio
async def test_route_task_returns_agent_calls(sample_agents):
    """REQ-06: route_task() использует Claude API и возвращает список AgentCall."""
    from services.ai_coordinator import route_task

    mock_response = _mock_claude_response(json.dumps([
        {"slug": "pdf-summarizer", "input": {"url": "test.pdf"}, "reason": "Нужен для суммаризации"},
    ]))

    with patch("services.ai_coordinator._client") as mock_client:
        mock_client.messages.create = AsyncMock(return_value=mock_response)
        result = await route_task("Summarize this PDF", sample_agents)

    assert isinstance(result, list)
    assert len(result) >= 1
    assert all(isinstance(call, AgentCall) for call in result)
    assert result[0].slug == "pdf-summarizer"


@pytest.mark.asyncio
async def test_route_task_filters_invalid_slugs(sample_agents):
    """REQ-06: route_task() отфильтровывает агентов с несуществующими slug."""
    from services.ai_coordinator import route_task

    mock_response = _mock_claude_response(json.dumps([
        {"slug": "pdf-summarizer", "input": {}, "reason": "OK"},
        {"slug": "nonexistent-agent", "input": {}, "reason": "Bad"},
    ]))

    with patch("services.ai_coordinator._client") as mock_client:
        mock_client.messages.create = AsyncMock(return_value=mock_response)
        result = await route_task("Test task", sample_agents)

    slugs = [call.slug for call in result]
    assert "pdf-summarizer" in slugs
    assert "nonexistent-agent" not in slugs


# ─── REQ-07: evaluate_output() возвращает QualityEvaluation ─────


@pytest.mark.asyncio
async def test_evaluate_output_returns_quality_evaluation(sample_agents):
    """REQ-07: evaluate_output() возвращает QualityEvaluation со score 0-100."""
    from services.ai_coordinator import evaluate_output

    mock_response = _mock_claude_response(json.dumps({
        "score": 85,
        "reasoning": "Качественная суммаризация",
    }))

    with patch("services.ai_coordinator._client") as mock_client:
        mock_client.messages.create = AsyncMock(return_value=mock_response)
        result = await evaluate_output(
            agent=sample_agents[0],
            input_data={"url": "test.pdf"},
            output_data={"summary": "Test summary"},
            execution_id="test-exec-001",
        )

    assert isinstance(result, QualityEvaluation)
    assert result.score == 85
    assert result.execution_id == "test-exec-001"
    assert result.reasoning == "Качественная суммаризация"


# ─── REQ-08: Пороговая логика complete/refund ────────────────────


@pytest.mark.asyncio
async def test_score_above_threshold_gives_complete(sample_agents):
    """REQ-08: score >= 70 → decision='complete'."""
    from services.ai_coordinator import evaluate_output

    mock_response = _mock_claude_response(json.dumps({
        "score": 75,
        "reasoning": "Достаточно хорошо",
    }))

    with patch("services.ai_coordinator._client") as mock_client:
        mock_client.messages.create = AsyncMock(return_value=mock_response)
        result = await evaluate_output(
            agent=sample_agents[0],
            input_data={},
            output_data={"result": "ok"},
            execution_id="exec-threshold-high",
        )

    assert result.decision == "complete"
    assert result.score >= 70


@pytest.mark.asyncio
async def test_score_below_threshold_gives_refund(sample_agents):
    """REQ-08: score < 70 → decision='refund'."""
    from services.ai_coordinator import evaluate_output

    mock_response = _mock_claude_response(json.dumps({
        "score": 45,
        "reasoning": "Низкое качество",
    }))

    with patch("services.ai_coordinator._client") as mock_client:
        mock_client.messages.create = AsyncMock(return_value=mock_response)
        result = await evaluate_output(
            agent=sample_agents[0],
            input_data={},
            output_data={"result": "bad"},
            execution_id="exec-threshold-low",
        )

    assert result.decision == "refund"
    assert result.score < 70


# ─── REQ-09: Score clamped to 0-100 (u8-safe for on-chain) ──────


@pytest.mark.asyncio
async def test_score_clamped_to_valid_range(sample_agents):
    """REQ-09: score > 100 clamped to 100, score < 0 clamped to 0."""
    from services.ai_coordinator import evaluate_output

    # Claude возвращает score=150 (невалидный)
    mock_response = _mock_claude_response(json.dumps({
        "score": 150,
        "reasoning": "Отличная работа",
    }))

    with patch("services.ai_coordinator._client") as mock_client:
        mock_client.messages.create = AsyncMock(return_value=mock_response)
        result = await evaluate_output(
            agent=sample_agents[0],
            input_data={},
            output_data={"result": "great"},
            execution_id="exec-clamp",
        )

    assert 0 <= result.score <= 100


# ─── Обработка ошибок ────────────────────────────────────────────


@pytest.mark.asyncio
async def test_route_task_raises_on_api_error(sample_agents):
    """route_task() выбрасывает CoordinatorError при ошибке Claude API."""
    from services.ai_coordinator import route_task
    import anthropic

    with patch("services.ai_coordinator._client") as mock_client:
        mock_client.messages.create = AsyncMock(
            side_effect=anthropic.APIConnectionError(request=MagicMock())
        )
        with pytest.raises(CoordinatorError):
            await route_task("Test", sample_agents)
```

Note: `pytest.ini` or `pyproject.toml` config for asyncio is NOT needed — `pytest-asyncio>=0.24.0` auto-detects `@pytest.mark.asyncio` by default. But add a `backend/pytest.ini` with:
```ini
[pytest]
asyncio_mode = auto
```
  </action>
  <verify>
    <automated>cd /Users/33karaulov/Documents/decentrathon5.0 && python3 -c "import ast; ast.parse(open('backend/tests/test_ai_coordinator.py').read()); print('syntax OK')" && grep -c "def test_" backend/tests/test_ai_coordinator.py</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/__init__.py` exists
    - File `backend/tests/test_ai_coordinator.py` exists
    - `grep -c "def test_" backend/tests/test_ai_coordinator.py` returns 7 (seven test functions)
    - `grep "REQ-06" backend/tests/test_ai_coordinator.py` returns matches
    - `grep "REQ-07" backend/tests/test_ai_coordinator.py` returns matches
    - `grep "REQ-08" backend/tests/test_ai_coordinator.py` returns matches
    - `grep "REQ-09" backend/tests/test_ai_coordinator.py` returns matches
    - `python3 -c "import ast; ast.parse(open('backend/tests/test_ai_coordinator.py').read())"` succeeds (valid Python)
    - Tests FAIL when run (because ai_coordinator.py doesn't exist yet): `cd backend && python3 -m pytest tests/test_ai_coordinator.py -x` exits non-zero
    - File `backend/pytest.ini` exists with `asyncio_mode = auto`
  </acceptance_criteria>
  <done>Test scaffold with 7 tests covering REQ-06 through REQ-09 exists; tests fail because ai_coordinator.py is not yet implemented</done>
</task>

</tasks>

<verification>
- `grep "anthropic" backend/requirements.txt` confirms dependency added
- `cd backend && python3 -c "from schemas.coordinator import AgentInfo, AgentCall, QualityEvaluation, CoordinatorError"` succeeds
- `grep "ANTHROPIC_API_KEY" backend/config.py` confirms config field
- `python3 -c "import ast; ast.parse(open('backend/tests/test_ai_coordinator.py').read())"` confirms valid test file
</verification>

<success_criteria>
- requirements.txt has anthropic, pytest, pytest-asyncio
- config.py has ANTHROPIC_API_KEY and AI_QUALITY_THRESHOLD
- schemas/coordinator.py has AgentInfo, AgentCall, QualityEvaluation, CoordinatorError
- tests/test_ai_coordinator.py has 7 tests covering all 4 requirements
- Tests fail because ai_coordinator.py does not exist yet (RED phase of TDD)
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-coordinator/02-01-SUMMARY.md`
</output>
